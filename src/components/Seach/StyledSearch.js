import { Select } from 'antd';
import styled from 'styled-components';

//all the custom styling for search will be here
const StyledSearch = styled(Select)`
  width: 20rem;
`;

export default StyledSearch;
